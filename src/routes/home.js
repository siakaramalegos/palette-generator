import { h } from "preact";
import Palette from "../components/Palette";

const Home = () => (
  <main class="home">
    <h1>Web Palette Generator</h1>
    <Palette />
  </main>
);

export default Home;
